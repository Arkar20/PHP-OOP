//!  when we create classes, we might have same functionality but w
<!-- differnt behavior or login  -->

//! indentify the duplicated methods and extract it to abstract class 
//! generalize the unique behavior or logic method (by this way the class that extends this class needs to define the method)